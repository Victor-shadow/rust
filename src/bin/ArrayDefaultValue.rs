fn main(){
    let arr:[i32; 10] = [-1; 10]; //an array of signed integer type 32 bits storing ten elements
    //the array stores an element of -1 ten times
    println!("array is{:?}", arr); //display the whole array
    println!("array size is :{}", arr.len()) //display the length of the array
}