fn main(){
    let arr = [100, 200, 300, 400, 500, 600, 700, 800, 900];
    let mut index = 0;
    //use of while constructor to loop over elements in a collection
    while index < 5 {
        println!("the value is: {}", arr[index]);

        index = index + 1;
    }

}